#include<stdio.h>

int main()
{
    int a[100],i,j,n,freq;
    printf("enter the size of array: ");
    scanf("%d",&n);

    printf("enter the elements of array: ");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
        freq[i] = -1;

        for(i=0;i<n;i++)
            count = 1;
        {
            for(j=i+1;j<n;j++)
                {
                    if(a[i]=a[j])
                    {
                        count++;
                        freq[j]=0
                    }
                }
        })
    }
}
